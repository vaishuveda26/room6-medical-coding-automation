# Backend (Python)

Suggested stack: FastAPI + Pydantic + SQLAlchemy.

## Key folders
- `app/api` route definitions
- `app/services` core medical automation logic
- `app/models` DB models
- `app/schemas` request/response schemas
- `tests/` unit and integration tests
